# these packages allow us to import very mature codes into our script to reduce the devt. process.
# pypi.org -- official python package repository.

# accessible through the cli.
# using "pip install"

# when you install python -- you automatically install pip / pip3.
# only on linux... 

# when you use pip install -- you also install the dependency for that package.

# check packages installed using "pip freeze".